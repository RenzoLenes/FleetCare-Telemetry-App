<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/byteCard"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/spacing_tiny"
    app:cardElevation="@dimen/elevation_card_small"
    app:cardCornerRadius="@dimen/corner_radius_small">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/spacing_small"
        android:gravity="center"
        android:minWidth="64dp">

        <!-- Índice del byte -->
        <TextView
            android:id="@+id/byteIndexText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
            android:textColor="@color/md_theme_light_onSurfaceVariant"
            tools:text="[0]" />

        <!-- Valor hexadecimal -->
        <TextView
            android:id="@+id/byteHexText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_tiny"
            android:textAppearance="@style/TextAppearance.Material3.TitleLarge"
            android:textStyle="bold"
            android:fontFamily="monospace"
            tools:text="1A" />

        <!-- Valor decimal -->
        <TextView
            android:id="@+id/byteDecText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_tiny"
            android:textAppearance="@style/TextAppearance.Material3.BodySmall"
            android:textColor="@color/md_theme_light_onSurfaceVariant"
            tools:text="(26)" />

        <!-- Indicador de tipo (estático/dinámico) -->
        <View
            android:id="@+id/byteTypeIndicator"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_marginTop="@dimen/spacing_tiny"
            android:background="@color/md_theme_light_primary" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
